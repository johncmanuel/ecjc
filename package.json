{
  "name": "ecjc",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "dotnet run --project apps/server",
    "dev": "trap 'kill 0' EXIT INT TERM; npm run dev:client & npm run dev:server & wait",
    "db:wipe": "dotnet ef database drop -f --project apps/server && dotnet ef database update --project apps/server",
    "gen:secret": "openssl rand -base64 32"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8"
  }
}
