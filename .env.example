# Note: In development (NODE_ENV !== "production"), the client automatically 
# proxies requests to http://localhost:5186 (aka the default dotnet development server).
API_URL=https://api.yourdomain.com

# PostgreSQL database connection string
ConnectionStrings__DefaultConnection="Host=localhost;Database=ecjc;Username=postgres;Password=yourpassword"

# Stripe API Keys (for optional broken streak penalties)
Stripe__SecretKey=sk_test_...
Stripe__WebhookSecret=whsec_...
